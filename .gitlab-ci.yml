stages:
  - test

.install-dependencies:
  image: mambaorg/micromamba:bookworm-slim
  before_script:
    - micromamba install --yes --name base --file environment.yml
    - micromamba clean --all --yes
    - micromamba run poetry config virtualenvs.create false --local
    - micromamba run poetry install --all-extras
    - export PYTHONPATH="$PYTHONPATH:src"

unit-tests:
  stage: test
  extends: .install-dependencies
  script:
    - micromamba run python -m unittest discover -s test -p "test_*.py"

docstring-tests:
  stage: test
  extends: .install-dependencies
  script:
    - micromamba run python test/doctests_runner.py
